[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "entityq"
version = "0.1.0"
description = "Financial entity data quality and automation framework"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
