# EntityQ Airflow Orchestration

## Purpose

This document explains how the EntityQ data quality pipeline can be orchestrated with Apache Airflow.

The local MVP can be run with:

```bash
python -m entityq.run_pipeline